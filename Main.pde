/* void setup() {
  size(500, 500);
  noStroke();
}

void draw() {
  background(204);
  float  x1 = map(mouseX, 0, width, 50, 150);
  float y1 = map(mouseY, 0, height, 50, 150);
  ellipse(x1, y1, 50, 50);  
  float x2 = map(mouseX, 0, width, 0, 200);
  float y2 = map(mouseY, 0, height, 0, 200);
  ellipse(x2, y2, 50, 50);  
} */

